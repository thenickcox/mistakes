= simple_form_for @post do |f|
  = f.error_notification
  .form-inputs
    = f.input :title
    = f.input :content
    = f.input :tag_list, label: 'Languages (comma separated)'

  .form-actions
    = f.button :submit
